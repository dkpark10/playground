import { defineConfig } from 'tsup';

export default defineConfig({
  entry: ['src/index.tsx'],  // 빌드할 파일 경로
  format: ['cjs', 'esm'],    // CommonJS 및 ESM 형식으로 번들
  dts: true,                 // TypeScript 선언 파일 생성
  minify: true,              // 번들 파일 최소화
  external: ['react', 'react-dom'],  // 번들링에서 React 제외
});
